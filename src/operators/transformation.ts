export {
  map as transformWith,
  buffer as collectUntilSignal,
  bufferCount as collectN,
  bufferTime as collectForDuration,
  bufferToggle as collectBetweenSignals,
  bufferWhen as collectUntilFactory,
  concatMap as transformSequentially,
  concatMapTo as switchToSequentially,
  exhaustAll as ignoreWhileBusy,
  exhaustMap as transformIfNotBusy,
  expand as expandRecursively,
  groupBy as groupInto,
  mapTo as replaceWith,
  mergeMap as transformConcurrently,
  mergeMapTo as mergeInto,
  mergeScan as accumulateConcurrently,
  pairwise as withPrevious,
  pluck as pickProperty,
  scan as accumulate,
  switchScan as accumulateSwitching,
  switchMap as transformSwitching,
  switchMapTo as switchTo,
  window as windowUntilSignal,
  windowCount as windowOfN,
  windowTime as windowForDuration,
  windowToggle as windowBetweenSignals,
  windowWhen as windowUntilFactory,
} from 'rxjs';
